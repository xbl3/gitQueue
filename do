@echo off 
setlocal enabledelayedexpansion 

set git=
set "good="

for %%a in ( git good ) do ( 
        if not defined %%a call :manGit
   
              for %%b in (!%%a!) do ( 
                if "%%a" == "git" ( 
                    for %%c in (%%b) do ( 
                      call :goodGame %%c
                      if not defined good ( 
                            set game=%%b )
              
    
    if "%%~a" == "good" 
  for %%b in (%%a) do if defined
    if not defined good git
set game=
set "lots="


https://github.com/olivierlemoal/mpv-spotify-script
  
    https://github.com/olivierlemoal/mpv-spotify-script.git
